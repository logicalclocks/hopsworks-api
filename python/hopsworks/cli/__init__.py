"""Hopsworks CLI — `hops` command-line interface.

The CLI is a user-facing Click application that wraps the Python SDK plus a
handful of REST escape hatches for endpoints the SDK does not expose.
Entry point: ``hopsworks.cli.main:main``.
"""
