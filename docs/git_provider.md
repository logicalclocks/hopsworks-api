# GitProvider API

You can obtain a GetApi handle via [`Project.get_git_api`][hopsworks.project.Project.get_git_api].
Once you have an API handle, you can use it to obtain GitProvider objects using the following methods:

::: hopsworks_common.core.git_api.GitApi.set_provider

::: hopsworks_common.core.git_api.GitApi.get_provider

::: hopsworks_common.core.git_api.GitApi.get_providers

::: hopsworks_common.git_provider.GitProvider
